# Shared fragments

These static fragments are provided so that a legacy or new dynamic publication database frontend can include the same header/footer and stylesheet as the Hugo site.

A PHP application, for example, could include these files and link `/assets/css/site.css`.
